using Pancake.Sound;
using VContainer;
using VContainer.Unity;

namespace Pancake.SceneFlow
{
    [EditorIcon("icon_entry")]
    public class MenuSceneScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
            base.Configure(builder);
        }
    }
}